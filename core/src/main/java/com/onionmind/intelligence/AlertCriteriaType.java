package com.onionmind.intelligence;

/** "Regras simples, não ML" (master-sdd sec. 6.6) — keyword/category/entity-type match, nothing trained. */
enum AlertCriteriaType {
    KEYWORD, CATEGORY, ENTITY_TYPE
}
